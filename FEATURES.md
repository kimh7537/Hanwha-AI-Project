# AudienceDeck AI — 기능 및 동작 방식

README가 "어떻게 실행하는가"라면, 이 문서는 **"무엇을 하고 내부에서 어떻게 동작하는가"**다.
모듈별 명세는 `docs/00`~`docs/10`에 있고, 여기서는 실제로 구현된 코드 기준으로 정리한다.

> 기준 시점: 백엔드 테스트 101개 통과, `/demo-check` 7항목 통과.

---

## 1. 한 줄 정의

기술문서를 업로드하면 **청중 / 발표 목적 / 발표 시간 / 필수 키워드 / 표현 스타일**에 맞춰
발표자료·발표 스크립트·예상 Q&A를 만들고, 그 결과가 **원문 근거를 벗어나지 않았는지 검증**한다.

범용 PPT 생성기가 아니다. 차별점은 두 가지뿐이다.

1. **청중별 재구성** — 같은 사실을 신입사원·실무자·임원·고객에게 다른 깊이로 설명한다.
   사실은 유지하고 표현의 깊이만 바꾼다.
2. **원문 근거 검증** — 모든 슬라이드에 원문 chunk 근거가 붙고, 생성 결과를 원문과
   기계적으로 대조해 근거 없는 주장·숫자 오류·의미 왜곡·조건 누락·민감정보를 잡아낸다.

이 도구는 발표 준비를 **가속**할 뿐 사람 검토를 대체하지 않는다.

---

## 2. 전체 동작 한 눈에

```
업로드 파일 (PDF / PPTX / TXT)
   │  parse_document → 페이지별 텍스트
   │  build_chunks   → chunk-01, chunk-02, … (근거 단위)
   ▼
SourceAnalysis        모듈 A  사실·수치·용어·조건 + 원문 근거
   ▼
AudienceContent       모듈 B  청중별 설명 · 용어 풀이 · 강조점 · 경고
   ▼
SlideDeck             모듈 C  제목 · takeaway · bullets · 시각자료 · 근거
   ▼
PresentationSupport   모듈 D  슬라이드별 스크립트 · 예상 Q&A · 리허설 카드
   ▼
VerificationReport    모듈 E  원문 대비 검증 (규칙 기반, 결정론적)
```

**단일 원본 원칙.** 청중별 결과물을 원문에서 각각 만들지 않는다. `SourceAnalysis`가 유일한
사실 원천이고 이후 모든 단계는 원문이 아니라 **앞 단계의 출력**을 입력으로 받는다.
그래서 청중을 바꿔도 수치와 사실은 그대로이고 설명의 깊이만 달라진다.

**AI와 규칙의 분담.** 문서 분석·자연어 재구성(A·B·C·D)은 LLM이, 원문 근거 대조·숫자 검사·
조건 누락 판정(E)은 결정론적 규칙 로직이 맡는다. 그래서 **검증 결과는 같은 입력에 항상 같다.**

구현: `backend/app/services/pipeline.py`

---

## 3. 사용자가 보는 기능

### 3.1 위저드 4단계 (`frontend/app/page.tsx`)

| 단계 | 화면 | 하는 일 |
|---|---|---|
| 1 | 업로드 | 파일 선택 → `POST /api/documents` → chunk 수·페이지 수 표시 |
| 2 | 조건 입력 | 청중·목적·시간·키워드·스타일·원어유지·슬라이드 수 |
| 3 | 생성 중 | 5단계 진행 표시. `generate`가 1~4단계, `verify`가 5단계 |
| 4 | 결과 | 탭 4개 + 다운로드 |

진행 표시의 5단계는 `문서 분석 → 청중 변환 → 슬라이드 설계 → 발표 지원 → 정확성 검증`이며,
실제 API 호출 진행도를 따른다. 실패하면 해당 단계에 ✕ 표시와 함께 **다시 시도 / 조건 수정** 버튼이 나온다.

### 3.2 결과 탭 4개 (`frontend/components/ResultView.tsx`)

- **발표자료** — 슬라이드별 제목 / takeaway 한 줄 / bullets / 추천 시각자료 / 근거 배지
- **발표 스크립트** — 슬라이드별 스크립트, "꼭 말할 것", 예상 발표 시간 합계 vs 목표 시간
- **예상 Q&A** — 청중별 질문 3~5개와 답변, 근거 배지. 근거로 답할 수 없는 질문은
  "원문 확인 필요"로 표시된다. 아래에 **리허설 카드**(보강이 필요한 지점)가 붙는다
- **정확성 검증** — 상태 배지 + 요약 + 지적 항목(심각도·유형·슬라이드·수정 제안) + `원문 확인 필요` 목록

**근거 배지**(`chunk-03`)를 클릭하면 그 chunk의 원문 문장과 페이지 번호가 뜬다.

### 3.3 다운로드 3종

| 형식 | 만드는 곳 | 내용 |
|---|---|---|
| **PPTX** | 백엔드 `export_pptx.py` | 표지 → 본문 슬라이드 → 부록(예상 Q&A) → 부록(검증). 발표자 노트에 스크립트 |
| **Markdown** | 프론트엔드 `lib/export.ts` | 슬라이드 + 스크립트 + Q&A + 검증 리포트 |
| **JSON** | 프론트엔드 | `GenerateResponse` 원본 그대로 |

PPTX는 이미 검증을 마친 결과를 배치만 하므로 **새 문장을 만들지 않고 LLM도 호출하지 않는다.**
python-pptx가 없는 환경에서는 export만 503(한국어 안내)으로 끝나고 나머지는 그대로 동작한다.

### 3.4 안전 장치가 화면에 드러나는 방식

- 고객 청중이면 상단에 **"공개 전 검토 필요"** 배지, PPTX 표지에도 같은 문구가 **글자로** 들어간다
  (색만으로 상태를 구분하지 않는다)
- LLM이 실패해 규칙 경로로 대체됐으면 "AI 응답에 실패해 기본 분석 결과로 대체했습니다 (사유)"가 뜬다
- 고객용 자료에 내부 정보·과장 표현이 남아 있으면 경고 목록이 뜬다
- 오류·로딩 메시지는 전부 한국어이며, 백엔드가 내려준 `detail`을 그대로 보여준다

---

## 4. 입력 조건

| 항목 | 값 | 비고 |
|---|---|---|
| 문서 | PDF / PPTX / TXT (`.md`, `.text` 포함) | 10MB 이하 |
| 청중 | 신입사원 · 실무자 · 임원 · 고객 | 결과 깊이를 결정하는 핵심 축 |
| 목적 | 교육 · 내부보고 · 기술설명 · 제안 | 덱 제목 접미사에 반영 |
| 발표 시간 | 3분 · 5분 · 10분 | 슬라이드 수와 스크립트 분량 결정 |
| 필수 키워드 | 쉼표 구분 자유 입력 | 덱에 반드시 등장해야 하며, 없으면 검증이 지적 |
| 스타일 | 전문적 · 간결 · 설득형 · 친절한 설명형 | `간결`이면 설명을 110자로 압축 |
| 원어 유지 | 켜기 / 끄기 | 끄면 `임베딩(embedding)` → `임베딩` |
| 슬라이드 수 | 자동 추천 또는 직접 지정 | 지정 시 3~10장으로 제한 |

미지원 형식(`.ppt`, `.doc`, `.docx`, `.hwp`, `.key`)은 **무엇으로 바꿔 오면 되는지**까지 안내한다.

---

## 5. 모듈별 동작 방식

각 모듈은 **LLM 경로**와 **휴리스틱 경로**를 모두 갖는다. LLM을 쓸 수 없거나 응답이 계약과
맞지 않으면 자동으로 휴리스틱으로 떨어지고, 그 사실은 `meta.fallback_used`에 남는다.

> **휴리스틱은 문장을 지어내지 않는다.** 원문 문장을 고르고 재배열할 뿐이다.
> mock이 캔드(canned) JSON을 반환했다면 검증 모듈이 의미를 잃기 때문이다.

### 5.0 문서 파싱과 청킹

**파싱** (`document_parser.py`)

- **PDF** — PyMuPDF. 페이지 단위
- **PPTX** — python-pptx. 슬라이드 1장 = 페이지 1개.
  제목을 맨 앞에 두고, 나머지 도형은 **읽기 순서(위→아래, 왼→오른쪽)**로 정렬한다
  (z-order 그대로 읽으면 제목이 본문 뒤로 간다). 표는 행 단위로 펴고 병합 셀 중복은 접는다.
  발표자 노트도 `[발표자 노트]`로 함께 읽는다
- **TXT** — `utf-8 → utf-8-sig → cp949 → euc-kr` 순으로 디코딩 시도.
  페이지 개념이 없으므로 1200자 단위 **가상 페이지**를 만든다 (근거에 페이지를 표시하기 위해)

텍스트를 못 찾으면 "스캔 이미지 PDF일 수 있습니다" 처럼 원인을 짚어 준다.

**청킹** (`chunking.py`) — 목표 450자 / 최대 700자 / 겹침 80자 / 최소 40자.
문단 경계를 우선하고, 너무 짧은 꼬리는 직전 chunk에 붙인다.
id는 `chunk-01` 형식이며 **이후 모든 `source_refs`가 이 id를 가리킨다.** 페이지 번호를 잃지 않는다.

chunk를 작게 유지하는 이유는 근거 배지를 눌렀을 때 **읽을 수 있는 한 덩어리**가 나와야 하기 때문이다.

### 5.1 모듈 A — 문서 분석 (`analyzer.py`)

**출력 `SourceAnalysis`**

| 필드 | 내용 |
|---|---|
| `core_message` | 문서의 핵심 한 문장 |
| `technical_points` | 기술 서술 문장 (최대 8) |
| `key_features` | 문서의 불릿 항목 |
| `numbers` | 수치 + 단위 + 그 수치가 나온 문장 |
| `terms` | 용어 + 정의 |
| `must_keep` | 반드시 유지해야 할 조건·주의사항 |
| `source_evidence` | chunk 원문 전체 (근거 조회용) |
| `unverified` | 근거를 못 찾아 넘긴 항목 |

**휴리스틱 경로가 문장을 고르는 방법**

- **핵심 메시지** — 키워드 겹침 ×3, `~시스템이다/서비스다` 패턴 +4, 서술 어미 +1, 길이 20~120자 +1.5
- **수치** — 문장에서 값·단위를 추출하고 그 문장을 `meaning`으로 남긴다. 값+단위 조합으로 중복 제거
- **용어** — `임베딩(embedding, 문장을 숫자 벡터로…)` 괄호형과 `A는 B이다` 정의문형.
  정의 표지(`이다/말한다/지표/평균`…)가 없는 서술문, 지시어로 시작하는 후보,
  어미 조각(`없`, `되`, `한`…)은 버린다
- **조건(must_keep)** — 금지·필수·전제 같은 **강한 조건이 앞**에 오도록 정렬
- **기술 항목** — 기술 표지(`동작`, `구조`, `방식`, `모듈`…) 개수 + 키워드 겹침 ×2로 점수화

**LLM 경로의 안전장치** — 계약의 모든 필드에 기본값이 있어서 형태가 전혀 다른 JSON도
'빈 분석'으로 통과한다. 그래서 `_is_substantive()`로 실질 내용이 있는지 확인하고,
없으면 휴리스틱으로 떨어진다. `source_evidence`는 LLM 응답을 쓰지 않고 **항상 우리가 채운다.**

**마지막에 근거 무결성 강제** (`evidence.py`) — 존재하지 않는 chunk id를 가리키는 항목은
삭제하지 않고 `unverified`로 옮긴다.

### 5.2 모듈 B — 청중 변환 (`audience.py`) · 차별화 ①

**절대 규칙: 사실은 유지하고 표현의 깊이만 바꾼다.**

청중마다 **설명 슬롯**이 다르다. 같은 `SourceAnalysis`에서 다른 항목을 다른 순서로 꺼낸다.

| 청중 | 설명 슬롯 | 용어 풀이 |
|---|---|---|
| 신입사원 | 무엇을 하는 기술인가 → 왜 필요한가 → 어떻게 동작하나 → 기억해야 할 조건 | **전부** |
| 실무자 | 기술 구성 → 성능과 측정 조건 → 적용 조건과 제약 | 2개 |
| 임원 | 결론 → 도입 효과 → 리스크와 전제 조건 → 판단에 필요한 기술 요약 | **0개** |
| 고객 | 제공하는 가치 → 적용 효과 → 동작 방식 → 적용 전 확인이 필요한 조건 | 2개 |

- 실무자에게는 `must_keep`을 **하나도 빠뜨리지 않는다** (다른 청중은 상위 2~3건)
- 임원·고객에게는 "도입 효과" 서술이 강한 수치가 앞에 오도록 정렬한다
- `원어 유지`를 끄면 설명·용어·Q&A 전부에서 영문 병기를 일관되게 제거한다
  (한 곳만 적용되면 용어 풀이는 "임베딩", 질문은 "임베딩(embedding)"이 되어 어긋난다)

**고객용 경고 `cautions`** — LLM 경로에서도 반드시 적용되도록 분리되어 있다.

- 내부 정보 표지가 남아 있으면 → 어떤 표현인지 나열하고 제거·승인 확인을 요구
- 과장 표지(`최고`, `완벽`, `무조건`, `100%`, `절대`, `유일한`, `혁신적`)가 있으면 경고
- 근거를 못 찾은 항목이 있으면 건수를 알림
- 항상 "고객 공개 자료입니다. 발표 전 담당자 검토가 필요합니다."

### 5.3 모듈 C — 슬라이드 구성 (`planner.py`)

**발표 시간 → 슬라이드 수**: 3분 → 4장, 5분 → 5장, 10분 → 7장.
`slide_count`를 주면 그 값을 우선하되 **3~10장으로 제한**한다.

**구성 순서**

1. 청중용 설명 하나당 슬라이드 하나. **근거가 없는 설명은 슬라이드로 만들지 않는다**
2. 같은 결론(takeaway)이 두 장에 반복되면 뒤엣것을 버린다
3. 결론 슬라이드는 앞 슬라이드들의 근거를 **합집합으로 승계**한다
4. 장수가 넘치면 잘라내되 **3분 발표는 결론을 맨 앞으로** 올린다 (결론 우선)
5. 장수가 모자라면 원문 사실로 보충한다 (핵심 수치 / 반드시 지켜야 할 조건 / 용어 정리)
6. **필수 키워드 보강** — 키워드가 덱에 없으면 그 키워드를 담은 원문 문장을 찾아,
   이미 같은 근거를 인용 중인 슬라이드에 근거와 함께 붙인다.
   원문에 아예 없으면 넣지 않고 검증이 `누락`으로 보고한다

> **bullet은 40자로 자르지 않는다.** 규칙만으로는 의미 압축이 불가능해 자르면 "줄었" 같은
> 깨진 어미가 남는다. 명세의 "40자 내외"는 LLM 경로 기준이다.

**LLM 경로** — 응답의 `source_refs` 중 실제 존재하는 것만 남기고, 근거가 하나도 없는
슬라이드는 제외한다(사유를 fallback 노트로 기록). 남은 게 없으면 휴리스틱으로 떨어진다.

### 5.4 모듈 D — 발표 지원 (`support.py`)

**스크립트** — 한국어 발화 속도를 **초당 5자**로 잡는다. 슬라이드당 배분 시간은
`발표시간 ÷ 슬라이드 수`이며 25~75초로 제한한다.

문장을 채우는 순서: `speaker_notes`(청중용 설명 원문) → `takeaway` →
**그 슬라이드가 실제로 인용한 chunk의 원문 문장**. 마지막 단계가 없는 내용을 지어내지 않으면서
발표 시간을 맞추는 유일한 방법이다. 고객 발표에서는 보강 문장에 내부 정보가 섞이지 않게 거른다.

bullets는 쓰지 않는다 — 설명을 잘라낸 형태라 그대로 읽으면 문장이 끊겨 들린다.
토큰 70% 겹침으로 같은 말 반복도 막는다.

**예상 Q&A (최대 5개)** — 청중별로 질문 세트가 다르다.

| 청중 | 질문 예 |
|---|---|
| 신입사원 | "○○이(가) 무슨 뜻인가요?" / "왜 필요한가요?" / "제 업무에서는 무엇이 달라지나요?" |
| 실무자 | "적용 조건은?" / "기존 방식과 비교하면?" / "예외 상황에서는?" / "측정 조건은?" |
| 임원 | "도입 효과를 수치로?" / "리스크·전제 조건은?" / "일정은?" / "비용은?" |
| 고객 | "도입하면 무엇이 달라지나요?" / "저희 환경에도 적용되나요?" / "성능은?" / "리스크는?" |

**근거로 답할 수 없으면 답을 지어내지 않고** `원문 확인 필요: (무엇이 없는지)`로 표시한다.
임원의 "도입 비용은?"은 원문에 비용 정보가 없다는 것을 드러내기 위해 의도적으로 비워 둔 질문이다.

**리허설 카드** — 근거로 답하지 못한 질문 **2개를 먼저** 카드로 만든다 (보강이 필요한 지점이므로).
그다음 이 청중이 가장 먼저 묻는 질문 1개를 더한다.

### 5.5 모듈 E — 검증 (`verifier.py`) · 차별화 ②

**전부 규칙 기반이다. LLM에 판정을 떠넘기지 않는다.** 같은 입력에는 항상 같은 결과가 나오고,
근거 없는 신뢰도 점수("정확도 92%")는 만들지 않는다.

| # | 검사 | 심각도 | 판정 방법 |
|---|---|---|---|
| 1 | 원문에 없는 주장 | 심각 | 슬라이드에 `source_refs`가 없거나, 존재하지 않는 chunk id를 가리킴 |
| 2 | 숫자·단위 오류 | 심각 | 슬라이드·스크립트의 숫자가 원문 어디에도 없음 |
| 3 | 의미 왜곡 | 주의 | 원문에서 조건이 붙은 수치를 슬라이드가 조건 없이 인용 |
| 4 | 과도한 단순화 | 주의 | `must_keep` 조건의 토큰이 덱에 50% 미만으로 반영됨 (상위 3건) |
| 5 | 핵심 내용 누락 | 주의 | 필수 키워드가 덱에 등장하지 않음 |
| 6 | 민감·내부 정보 | 주의 | 고객 청중일 때 슬라이드+스크립트에 내부 정보·과장 표현 |

**상태**: 심각 1건 이상 → `검토 필요` / 주의만 → `주의` / 없으면 → `확인됨`.

두 가지 구현상의 판단:

- **숫자 대조는 단위까지 포함한 토큰으로만 한다.** 값만 비교하면 "3만 건"의 3이
  "3개 부서"의 3과 겹쳐 오탐이 난다
- **조건부 수치는 요약이 아니라 원문 문장을 직접 훑는다.** 같은 값이 여러 번 나오는 문서에서는
  `numbers`의 `meaning`이 조건이 없는 쪽 문장일 수 있기 때문이다
- 민감정보 검사는 슬라이드뿐 아니라 **발표 스크립트도** 본다. 발표자가 실제로 입 밖에 내는 문장이므로

---

## 6. 근거(source_refs)는 어떻게 보장되는가

데모 성공 기준 4번 — **모든 슬라이드에 원문 근거가 하나 이상** — 을 지키는 장치가 4겹이다.

1. **모듈 A 끝** — `enforce_analysis_evidence()`가 유효하지 않은 근거를 가진 항목을
   삭제하지 않고 `unverified`로 옮긴다
2. **모듈 B·C·D** — LLM이 만들어낸 chunk id를 `valid_refs()`로 걸러 실제 존재하는 것만 남긴다
3. **모듈 C** — 근거가 없는 설명은 슬라이드로 만들지 않고, 결론 슬라이드는 앞 슬라이드의
   근거를 `inherit_refs()`로 승계한다
4. **모듈 E** — 그래도 근거가 없으면 `심각` 등급으로 보고한다

**근거를 못 찾은 항목은 삭제하지 않는다.** 삭제하면 사라진 사실을 아무도 모르지만,
`unverified`로 남기면 화면의 "원문 확인 필요" 목록에 드러난다.

`/evidence-audit` 스킬이 생성 결과 JSON을 받아 이 무결성을 다시 감사한다.

---

## 7. LLM provider와 fallback

```
LLM_PROVIDER=mock        (기본)  → 규칙 기반 휴리스틱 경로
LLM_PROVIDER=anthropic            → Claude
LLM_PROVIDER=openai               → OpenAI
```

- **API 키가 없으면 provider 이름과 무관하게 조용히 mock으로 떨어진다.** 데모는 멈추지 않는다
- 호출 실패·타임아웃·JSON 파싱 실패·계약 불일치 — **어떤 실패든** 해당 단계만 휴리스틱으로
  대체되고 파이프라인은 계속 진행된다 (`RunContext.call_json()`이 `None`을 반환)
- 첫 실패 사유가 `meta.fallback_reason`에 남아 화면 상단에 그대로 표시된다
- 응답 파싱은 코드펜스(```json)와 서두 설명을 견딘다
- **원문 전체를 프롬프트에 넣지 않는다.** chunk 단위로 처리하고 상한은 12,000자다
- **API 키는 백엔드에만 둔다.** 모든 LLM 호출은 FastAPI 안에서만 일어난다

`GET /api/health`로 현재 어떤 경로로 동작 중인지 확인할 수 있다.

---

## 8. API

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/health` | provider 모드(`mock`/`anthropic`/`openai`)와 Chroma 사용 여부 |
| POST | `/api/documents` | 업로드(multipart) → 파싱·청킹 → `{document, chunks}` |
| POST | `/api/presentations/generate` | 모듈 A~E 전체 실행 → `GenerateResponse` |
| POST | `/api/presentations/verify` | 저장된 결과 재검증, 또는 넘겨받은 덱 검증 |
| GET | `/api/presentations/{id}` | 저장된 결과 조회 |
| GET | `/api/presentations/{id}/export/pptx` | PPTX 파일 다운로드 |

- `generate`가 검증 리포트까지 함께 돌려준다. `verify`는 자료를 수정한 뒤 다시 검사할 때 쓴다
- 오류 `detail`은 **사용자에게 그대로 보여줄 수 있는 한국어**다 (프론트엔드가 그대로 표시한다)
- PPTX 파일명이 한국어라 `Content-Disposition`에 ASCII 대체 이름과 RFC 5987 이름을 함께 담고,
  CORS `expose_headers`로 노출한다

**저장은 인메모리다.** 서버를 재시작하면 업로드한 문서와 생성 결과가 사라진다 (`store.py`).

---

## 9. 데이터 계약

계약은 **6인 병렬 개발의 경계**이며 항상 4곳이 함께 움직인다.

```
backend/app/models/contracts.py   Pydantic  (원본)
frontend/lib/types.ts             TypeScript 미러
backend/fixtures/*.json           단계별 fixture
docs/01-contracts.md              문서
```

한쪽만 고치면 `contract-guard` 훅이 나머지 3곳을 상기시키고, `/contract-sync` 스킬이 동기화한다.
`frontend/lib/labels.ts`가 화면 라벨의 유일한 원본이고, PPTX용 백엔드 미러
`app/services/labels.py`는 `tests/test_labels_mirror.py`가 대조한다.

---

## 10. 실행과 검사

```powershell
# 백엔드 (http://localhost:8000)
cd backend; .venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000

# 프론트엔드 (http://localhost:3000)
cd frontend; npm run dev

# 테스트 101개
cd backend; .venv\Scripts\python.exe -m pytest

# 프론트엔드 타입 검사 (tsc 단독은 Next 생성 타입 때문에 실패한다)
cd frontend; npm run build
```

스킬 스크립트 (저장소 루트에서):

```powershell
backend\.venv\Scripts\python.exe .claude\skills\demo-check\run_demo.py        # 데모 기준 7항목
backend\.venv\Scripts\python.exe .claude\skills\contract-sync\check_contracts.py
backend\.venv\Scripts\python.exe backend\scripts\build_fixtures.py            # 계약 변경 후
```

> `backend/.env`에 `LLM_PROVIDER`가 `mock` 이외로 설정되어 있으면 `test_api.py`의
> provider 단언 2건이 실패한다. 테스트는 `LLM_PROVIDER=mock`으로 돌린다.

---

## 11. 데모 성공 기준 7항목

`/demo-check` 스킬이 이 목록을 실제 실행으로 검사한다.

1. 기술문서 하나를 업로드한다
2. `고객 / 기술설명 / 5분 / 설득형`을 선택한다
3. 5장 안팎의 발표자료가 생성된다
4. **모든 슬라이드에 원문 근거가 하나 이상** 있다
5. 스크립트와 예상 Q&A가 생성된다
6. 검증 탭에서 최소 1건의 "주의" 또는 "확인됨" 결과가 **설명 가능하게** 나온다
7. 같은 문서를 `신입사원`으로 바꾸면 용어와 설명 깊이가 명확히 달라진다

---

## 12. 구현 범위

**구현됨** — 업로드·파싱(PDF/PPTX/TXT)·청킹, 모듈 A~E 전체, mock↔실 LLM 전환과 자동 fallback,
위저드 UI 4단계, 결과 4개 탭, 근거 클릭 조회, 고객용 경고,
**PPTX/Markdown/JSON 다운로드**, 백엔드 테스트 101개.

**미구현(선택)** — Chroma Cloud 임베딩 검색(현재는 keyword/메모리), DOCX 입력,
결과 영구 저장, 실시간 AI 관객 대화(현재는 예상 질문 카드).

---

## 13. 되돌리기 전에 이유를 확인할 판단들

코드가 "덜 다듬어진 것처럼" 보이지만 의도적으로 그렇게 둔 지점들이다.

| 판단 | 이유 |
|---|---|
| 휴리스틱은 문장을 지어내지 않고 원문 문장을 고르고 재배열한다 | mock이 캔드 JSON을 반환하면 검증 모듈이 의미를 잃는다 |
| 슬라이드 bullet을 40자로 자르지 않는다 | 규칙만으로는 의미 압축이 불가능해 "줄었" 같은 깨진 어미가 남는다 |
| LLM 응답을 Pydantic 검증만으로 믿지 않는다 | 모든 필드에 기본값이 있어 형태가 다른 JSON도 '빈 결과'로 통과한다 |
| 숫자 대조는 단위까지 포함한 토큰으로만 한다 | "3만 건"의 3이 "3개 부서"의 3과 겹쳐 오탐이 난다 |
| PPTX export는 새 문장을 만들지 않는다 | 파일 안에서 요약하면 `source_refs` 대응이 깨진다. 넘치면 글자를 줄인다 |
| 근거 없는 항목을 삭제하지 않고 `unverified`로 넘긴다 | 삭제하면 사라진 사실을 아무도 모른다 |
| 상태를 색만으로 구분하지 않는다 | 화면·PPTX 모두 텍스트 라벨을 병행한다 |
