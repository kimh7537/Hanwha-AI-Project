# AudienceDeck AI - Claude Code 구현 명세서

## 0. Claude Code에게 요청하는 역할

당신은 4일 안에 완성해야 하는 팀 프로젝트의 수석 풀스택 개발자다. 아래 명세를 기준으로 동작하는 MVP를 구현하라. 작업을 시작하기 전에 전체 계획, 파일 트리, 환경변수 목록을 짧게 제시한 뒤 구현한다. 명세에 없는 세부사항은 **데모 안정성, 단순성, 원문 근거 보존**을 우선해 합리적으로 결정한다.

중요: 이 프로젝트의 목표는 범용 PPT 생성기가 아니다. **하나의 기술문서를 청중과 발표 조건에 맞게 재구성하고, 발표 스크립트·예상 Q&A·원문 대비 검증까지 제공하는 AI 발표 지원 워크플로우**를 만드는 것이다.

---

## 1. 프로젝트 한 문장 정의

기술문서를 입력하면 청중, 발표 목적, 발표 시간, 필수 키워드, 표현 스타일에 맞춰 발표자료와 발표 스크립트·예상 Q&A를 생성하고, 생성 결과가 원문 근거를 벗어나지 않았는지 검증하는 AI 발표 지원 솔루션.

서비스 이름은 임시로 `AudienceDeck AI`를 사용한다.

### 핵심 메시지

> 자료를 만드는 AI가 아니라, 청중이 이해할 때까지 리허설하는 AI.

---

## 2. 4일 MVP 범위

### 반드시 구현할 흐름

```text
기술문서 업로드
  -> 발표 조건 설정
  -> 원문 핵심 정보 추출
  -> 청중 맞춤 내용 변환
  -> 슬라이드 구성/미리보기 생성
  -> 발표 스크립트 및 예상 Q&A 생성
  -> 원문 대비 정확성 검증
```

### MVP 입력값

1. 기술문서: PDF 또는 TXT를 우선 지원한다. DOCX/PPTX는 시간이 남을 때 추가한다.
2. 청중: `신입사원 | 실무자 | 임원 | 고객`
3. 발표 목적: `교육 | 내부보고 | 기술설명 | 제안`
4. 발표 시간: `3분 | 5분 | 10분`
5. 필수 키워드: 쉼표로 구분한 자유 입력
6. 표현 스타일: `전문적 | 간결 | 설득형 | 친절한 설명형`
7. 원어 유지: `켜기 | 끄기`
8. 원하는 슬라이드 수: 기본값은 발표 시간에 따라 자동 추천한다.

### MVP 출력값

1. 슬라이드 미리보기(제목, 핵심 문장, 본문 bullet, 원문 근거)
2. 슬라이드별 발표 스크립트
3. 예상 질문 3~5개와 권장 답변
4. 원문 대비 검증 리포트
5. 가능하면 PPTX 다운로드. 시간이 부족하면 HTML 슬라이드 미리보기와 JSON/Markdown 다운로드를 먼저 완성한다.

### 이번 MVP에서 제외할 것

- 로그인, 권한 체계, 결제
- 실제 회사 메일/드라이브/메신저 연동
- 여러 사용자의 동시 편집
- 완전한 디자인 편집기
- Gamma 등 외부 PPT 서비스 연동. API 자격증명이 이미 제공된 경우에만 추가한다.
- 근거 없는 수치 기반의 "정확도 92%" 같은 고정 점수 표기

---

## 3. 평가 기준 반영 (첨부 평가표)

첨부된 평가표는 구현 명령이 아니라 최종 결과물을 평가하는 기준이다. 구현과 발표는 아래 100점 기준을 직접 증명하도록 구성한다.

| 평가 항목 | 세부 항목 | 배점 | 구현/발표에서 보여줄 근거 |
|---|---|---:|---|
| AI 도구 활용의 적정성 | 적합한 AI 툴 선택 및 활용 능력 | 25 | 문서 분석·청중 변환·검증처럼 AI가 강점을 갖는 작업에 LLM을 사용하고, 각 단계의 입력/출력을 설명 |
| AI 도구 활용의 적정성 | 가공된 산출물의 퀄리티 | 25 | 청중별로 실제 내용 깊이와 표현이 달라지는 발표자료·스크립트·Q&A, 출처가 붙은 결과 |
| 문제 해결력 | 논리적 원인 분석 | 15 | 기존 발표 준비의 병목(자료 읽기, 구조 작성, 대본·Q&A 준비, 사실 확인)을 Before 화면과 시간 측정으로 제시 |
| 문제 해결력 | 데이터 근거 해결책 도출 | 15 | 동일 문서를 대상으로 기존 방식과 AI 방식의 소요시간·수정 횟수·오류 건수를 비교 |
| 현업 적용 가능성 | 실질적 기대효과 | 10 | 신입 교육, 팀 보고, 고객 제안 등 실제 사용 시나리오와 절감 가능한 준비시간을 제시 |
| 현업 적용 가능성 | 현실적 구현 가능성 | 10 | 업로드 문서 기반 MVP, 사람 검토 단계, mock fallback, 향후 시스템 연동 계획을 구분 |
| **총점** |  | **100** |  |

### 평가표에 맞춘 필수 산출물

- **AI 도구 선택 근거:** 문서 분석과 자연어 재구성은 LLM, 원문 근거·숫자 검사는 규칙/검증 로직을 함께 사용한다.
- **산출물 품질 비교:** 같은 원문을 신입사원·임원·고객용으로 생성해 차이를 나란히 보여준다.
- **원인 분석 자료:** 기존 작업을 `자료 읽기 → 구조 작성 → PPT 작성 → 스크립트 → Q&A`로 나누고 각 시간을 측정한다.
- **데이터 기반 효과:** 예시값과 실제 측정값을 구분한다. 최소 3회 테스트의 중앙값을 Before/After에 사용한다.
- **현업 적용 장치:** 모든 슬라이드에 원문 출처를 표시하고, 고객용 결과에는 내부정보 검토 경고를 넣는다.
- **구현 가능성 설명:** 이번 데모에서 구현한 범위와 향후 메일·드라이브·사내 템플릿 연동을 발표에서 분리한다.

---

## 4. 추천 기술 스택 및 배포

### 구현 스택

- Frontend: Next.js (App Router), TypeScript, Tailwind CSS
- Backend: FastAPI, Python, Pydantic
- AI 호출: Claude 또는 OpenAI 중 환경변수로 선택 가능한 provider adapter
- 문서 추출: PyMuPDF(PDF), plain text parser
- 검색/RAG: MVP는 작은 문서를 대상으로 chunk + keyword/embedding retrieval. Chroma Cloud가 설정된 경우 사용하고, 없으면 메모리/JSON fallback으로 동작한다.
- 슬라이드 렌더링: 우선 React 기반 HTML preview. 여유가 있으면 `python-pptx`로 PPTX export 추가.
- 테스트: pytest(backend), 최소한의 component/API smoke test(frontend)

### 권장 배포 구조

```text
Browser
  -> Next.js frontend (Vercel)
  -> FastAPI backend (Render 또는 Railway)
       -> LLM API
       -> Chroma Cloud (선택)
       -> 파일 저장소 또는 개발용 로컬 저장
```

프로토타입을 로컬에서 즉시 시연할 수 있어야 한다. 배포 환경변수나 Chroma Cloud가 없어도, 제공된 샘플 문서와 mock provider로 핵심 화면을 데모할 수 있도록 구현한다.

---

## 5. 가장 중요한 설계 원칙 - 단일 원본, 청중별 변환

청중별 발표자료를 각각 원문에서 바로 생성하지 않는다. 먼저 문서의 사실을 구조화한 `SourceAnalysis`를 만들고, 모든 다음 모듈이 이를 재사용한다.

```text
원문 문서
  -> SourceAnalysis (사실/수치/용어/근거)
  -> AudienceContent (청중별 설명)
  -> SlideDeck (발표 구조)
  -> PresentationSupport (스크립트/Q&A)
  -> VerificationReport (원문 대비 검증)
```

이 구조의 목적은 다음과 같다.

- 같은 사실을 신입·실무자·임원·고객에게 다른 깊이와 언어로 설명한다.
- 숫자와 핵심 주장은 원문 근거를 추적할 수 있어야 한다.
- 고객용에서는 내부 조직명, 미확인 수치, 민감한 표현을 별도 경고한다.
- 모듈마다 결과 형식이 고정되어 팀원이 병렬 개발해도 마지막에 쉽게 연결된다.

---

## 6. 공통 데이터 계약

프론트엔드와 백엔드는 아래 구조를 기준으로 타입/스키마를 만들고, 샘플 fixture를 함께 제공한다. Python Pydantic 모델과 TypeScript 타입은 의미상 동일해야 한다.

```json
{
  "presentation_request": {
    "audience": "customer",
    "purpose": "technical_explanation",
    "duration_minutes": 5,
    "keywords": ["정확도", "도입 효과"],
    "style": "persuasive",
    "preserve_original_terms": true,
    "slide_count": 5
  },
  "source_analysis": {
    "core_message": "",
    "technical_points": [],
    "key_features": [],
    "numbers": [
      {"value": "", "meaning": "", "source_refs": ["chunk-03"]}
    ],
    "terms": [
      {"term": "", "definition": "", "source_refs": ["chunk-02"]}
    ],
    "must_keep": [],
    "source_evidence": [
      {"id": "chunk-01", "text": "", "page": 1}
    ]
  },
  "slide_deck": {
    "title": "",
    "slides": [
      {
        "id": "slide-1",
        "title": "",
        "takeaway": "",
        "bullets": [],
        "visual_suggestion": "",
        "speaker_notes": "",
        "source_refs": ["chunk-01"]
      }
    ]
  }
}
```

---

## 7. 기능 모듈 상세

### A. 문서 분석 AI

입력 문서에서 아래 항목만 뽑는다.

- 핵심 메시지
- 핵심 기술 및 주요 특징
- 수치와 단위
- 전문용어와 쉬운 정의
- 반드시 유지해야 하는 조건/주의사항
- 모든 항목의 원문 근거(`chunk id`, 페이지, 짧은 인용)

규칙:

- 문서에 없는 사실은 추가하지 않는다.
- 숫자는 원문에 있는 값만 사용한다.
- 근거를 찾지 못한 내용은 `unverified`로 반환한다.

### B. 청중 맞춤 변환 AI - 핵심 차별화 기능

동일한 `SourceAnalysis`를 청중별로 다르게 설명한다.

| 청중 | 변환 원칙 |
|---|---|
| 신입사원 | 용어를 풀고, 비유/업무 예시를 넣으며, 배경부터 설명 |
| 실무자 | 기술 세부사항과 조건을 유지 |
| 임원 | 결론, 효과, 리스크, 의사결정 포인트 우선 |
| 고객 | 고객 가치와 적용 효과 중심. 내부 정보나 과장 가능 표현을 경고 |

모든 청중 버전은 **사실은 유지하고 표현의 깊이만 바꿔야 한다.**

### C. Presentation Planner AI

입력 조건과 변환된 내용을 받아 `SlideDeck` JSON을 생성한다.

권장 기본 구성:

1. 발표 목적/문제 배경
2. 기술 또는 해결 방식
3. 작동 원리/핵심 내용
4. 주요 장점 또는 가치
5. 결론 및 다음 행동

시간에 따른 원칙:

- 3분: 3~4장, 결론 우선
- 5분: 5장, 핵심 원리와 효과 포함
- 10분: 7~8장, 배경·사례·리스크를 추가

### D. 발표 지원 AI

슬라이드별로 아래를 생성한다.

- 30~60초 분량의 발표 스크립트
- 발표자가 꼭 말해야 할 한 문장
- 예상 질문 3~5개
- 질문별 권장 답변

예상 질문은 청중에 따라 달라져야 한다. 예를 들어 고객은 도입 효과와 리스크를, 임원은 비용·일정·의사결정 포인트를 더 많이 묻도록 한다.

### E. 검증 AI - 차별화 기능

최종 슬라이드/스크립트/Q&A를 원문 근거와 비교하여 다음을 검사한다.

1. 원문에 없는 주장
2. 숫자 또는 단위 오류
3. 의미 왜곡 또는 과도한 단순화
4. 중요한 핵심 내용 누락
5. 고객용 자료의 민감/내부 정보 위험

리포트 형식 예시:

```json
{
  "summary": "근거 확인이 필요한 문장 1건",
  "status": "warning",
  "items": [
    {
      "severity": "warning",
      "slide_id": "slide-3",
      "type": "oversimplification",
      "message": "조건 X가 생략되어 표현이 과도하게 단순화되었습니다.",
      "source_refs": ["chunk-04"],
      "suggested_fix": "조건 X를 bullet에 추가하세요."
    }
  ]
}
```

UI에서는 `확인됨`, `주의`, `검토 필요` 세 가지 상태로 보여준다. 신뢰도 점수는 산식이 명확히 구현된 경우에만 표시한다.

### F. AI 관객 리허설 - 있으면 좋은 기능

완성된 발표자료를 대상으로 AI가 선택한 청중 역할을 연기한다.

- "이 용어가 무슨 뜻인가요?" 같은 신입 질문
- "그래서 일정/효과는?" 같은 임원 질문
- "우리 회사에 적용하면 무엇이 달라지나요?" 같은 고객 질문

MVP에서는 실시간 대화가 아니라 `예상 질문 카드 + 보강 추천 슬라이드` 형태면 충분하다.

---

## 8. UI/UX 요구사항

한 화면에서 위저드처럼 진행되는 단순한 흐름을 만든다.

```text
[1. 문서 업로드]
       ↓
[2. 발표 조건]
청중 / 목적 / 시간 / 키워드 / 스타일 / 원어 유지 / 슬라이드 수
       ↓
[3. 생성 중]
문서 분석 -> 청중 변환 -> 슬라이드 설계 -> 발표 지원 -> 검증
       ↓
[4. 결과]
탭: 발표자료 | 발표 스크립트 | 예상 Q&A | 정확성 검증
```

결과 화면 요구사항:

- 각 슬라이드에 제목, takeaway, 본문 bullet, 추천 시각자료, 원문 근거를 표시한다.
- 원문 근거를 클릭하면 해당 chunk의 원문 문장과 페이지를 보여준다.
- 검증 탭은 색만으로 상태를 구분하지 말고 텍스트 라벨도 함께 보여준다.
- 고객 청중을 선택하면 "공개 전 검토 필요" 배지를 표시한다.
- 로딩 단계와 오류 원인을 사용자 친화적인 한국어로 표시한다.

디자인은 과한 효과보다 깔끔한 업무 도구처럼 만든다. 오렌지 포인트 색상을 제한적으로 사용해 Work Diet 콘셉트와 연결한다.

---

## 9. API 초안

```text
POST /api/documents
  - 파일 업로드 및 chunk 생성

POST /api/presentations/generate
  - PresentationRequest + document_id
  - SourceAnalysis, AudienceContent, SlideDeck, PresentationSupport 반환

POST /api/presentations/verify
  - document_id + SlideDeck + PresentationSupport
  - VerificationReport 반환

GET /api/presentations/{id}
  - 저장된 결과 조회 (선택)

POST /api/presentations/{id}/export/pptx
  - 시간이 남는 경우에만 구현
```

API 키는 프론트엔드에 노출하지 않는다. 모든 LLM 호출은 FastAPI에서 수행하며 `.env.example`에는 변수 이름만 포함한다.

```text
LLM_PROVIDER=
LLM_API_KEY=
CHROMA_API_KEY=
CHROMA_TENANT=
CHROMA_DATABASE=
NEXT_PUBLIC_API_BASE_URL=
```

---

## 10. 6인 역할 분담

1. 문서 분석: parser, chunking, `SourceAnalysis`
2. 청중 변환: audience prompt, `AudienceContent`
3. 발표 구조: `SlideDeck`, HTML preview, 선택적 PPTX export
4. 발표 지원: speaker notes, 예상 Q&A, 관객 리허설 카드
5. 검증: 원문 근거 비교, 숫자/왜곡/누락 검사
6. 통합/UI/PM: 입력 위저드, API 연결, 데모 시나리오, 테스트, 발표자료

모든 역할은 공통 fixture 문서 하나로 먼저 테스트한다. 각 모듈은 다음 모듈에 넘길 JSON 파일을 보유해야 한다.

---

## 11. 개발 일정과 완료 조건

### Day 1 - 구조 고정 및 각 모듈 첫 성공

- 공통 샘플 기술문서 1개 선정
- JSON/Pydantic/TypeScript 계약 확정
- 업로드/문서 추출, 청중 변환, 슬라이드 생성, Q&A, 검증 각각 최소 1회 성공

완료 조건: 각 모듈이 독립적으로 sample fixture를 입력받아 유효한 JSON을 반환한다.

### Day 2 - End-to-End 연결

- `문서 -> 조건 입력 -> 슬라이드 결과`를 한 번에 실행
- 실패 시 mock provider fallback으로도 데모가 가능하게 함

완료 조건: 디자인이 단순해도 전체 워크플로우가 끊기지 않는다.

### Day 3 - 차별화와 UI

- 발표 스크립트, 예상 Q&A, 원문 검증 탭 추가
- 원문 근거 표시
- 고객용 민감 정보 경고
- 실제 문서 3개 이상으로 테스트

### Day 4 - 새 기능 금지, 시연 안정화

- 오류 수정
- 고정 데모 시나리오 작성
- Before/After 시간 측정
- 3분 발표 버전과 영상 준비

### 최종 데모 성공 기준

1. 하나의 기술문서를 업로드한다.
2. `고객 / 기술 설명 / 5분 / 설득형`을 선택한다.
3. 5장 안팎의 발표자료가 생성된다.
4. 모든 슬라이드에 원문 근거가 하나 이상 있다.
5. 스크립트와 예상 Q&A가 생성된다.
6. 검증 탭에서 적어도 하나의 "주의" 또는 "확인됨" 결과가 설명 가능하게 나온다.
7. 같은 문서를 `신입사원`으로 바꾸면 용어와 설명 깊이가 명확히 달라진다.

---

## 12. 품질 및 안전 요구사항

- AI가 모르는 사실을 그럴듯하게 만들지 않도록, 근거 없는 문장은 반드시 "원문 확인 필요"로 표시한다.
- 수치는 원문 chunk와 연결하고, 숫자/단위가 맞는지 검증한다.
- 고객용 결과에서는 내부 정보일 수 있는 표현을 제거하지 못하면 경고한다.
- 긴 문서는 chunk 단위로 처리하고, 원문 전체를 무제한 프롬프트에 넣지 않는다.
- API 실패, 지원하지 않는 파일, 빈 문서는 명확한 오류 메시지를 반환한다.
- README에 로컬 실행, 환경변수 설정, mock mode 실행법, 배포 방법을 한국어로 작성한다.

---

## 13. Claude Code 작업 순서

1. 현재 저장소를 먼저 검사하고 기존 코드/설정을 존중한다.
2. 없으면 위 구조에 맞는 모노레포 또는 단순한 `frontend/`, `backend/` 구조를 만든다.
3. 공통 JSON fixture와 타입부터 만든다.
4. mock LLM provider로 End-to-End 흐름을 먼저 통과시킨다.
5. 실제 LLM provider를 추가한다.
6. UI를 연결하고, API/문서 파싱/검증에 대한 최소 테스트를 추가한다.
7. 로컬에서 실행해 데모 흐름을 검증한다.
8. 구현 결과, 실행 명령, 필요한 환경변수, 남은 선택 작업을 짧게 정리한다.

구현 중 외부 API 키나 배포 계정이 없으면, 이를 이유로 핵심 기능을 멈추지 않는다. mock mode와 샘플 기술문서로 완전한 데모를 우선 완성한다.
