# 가상환경은 윈도우용 바이너리라 리눅스 이미지에서 쓸 수 없고, 수백 MB 를 빌드 컨텍스트로
# 올려 배포를 느리게 만든다. 의존성은 Dockerfile 이 requirements.txt 로 다시 설치한다.
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.omc/

# 비밀정보는 이미지에 굽지 않는다. 배포 환경변수는 Render 대시보드에서 온다.
.env
